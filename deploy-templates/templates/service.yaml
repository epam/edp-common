apiVersion: v1
kind: Service
metadata:
  name: {{ include "edp-common.fullname" . }}
  labels:
    {{- include "edp-common.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "edp-common.selectorLabels" . | nindent 4 }}
